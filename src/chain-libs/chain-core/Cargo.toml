[package]
name = "chain-core"
version = "0.1.0"
authors = ["dev@iohk.io"]
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
cfg-if = "0.1"
quickcheck = { version = "0.9", optional = true }

[dev-dependencies]
quickcheck = "0.9"

[features]
property-test-api = ["quickcheck"]
